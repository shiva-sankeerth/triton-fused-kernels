# Core — let torch's own triton dependency resolve the version
# torch is pre-installed on Colab; listed here as a constraint only
torch>=2.1.0
numpy>=1.24.0,<2.0.0

# Benchmark runner
pandas>=2.0.0
tqdm>=4.65.0

# Analysis — plot generation (local, no GPU required)
matplotlib>=3.7.0
seaborn>=0.12.0
